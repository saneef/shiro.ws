<style webc:scoped>
	:host {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: var(--space-xs) var(--space-s);
		padding-block: var(--space-s);
		position: sticky;
		top: 100vh;
		color: var(--color-dark-tint);
	}
</style>

<footer webc:root="override" class="container">
	<slot></slot>
</footer>
