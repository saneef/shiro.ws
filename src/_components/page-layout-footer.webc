<style webc:scoped>
	:host {
		position: sticky;
		top: 100vh;
		padding-block: var(--space-xl) var(--space-m);
		color: var(--color-dark-tint);
	}

	:host .row {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: var(--space-xs) var(--space-s);
	}
</style>

<footer webc:root="override" class="container flow">
	<nav class="row">
		<slot name="nav"></slot>
	</nav>

	<div class="row">
		<slot></slot>
	</div>
</footer>
