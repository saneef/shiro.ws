<style webc:scoped>
	:host {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(min(100%, 300px), 1fr));
		grid-gap: var(--space-xl, 1rem);
		padding: 0;
	}
</style>

<ul webc:root="override" role="list">
	<slot></slot>
</ul>
